Make the dashboard simple and easy to use and navigate, add instruction and help guidelines or tips to use the dashboard. Make it more interactive and user friendly. Right now its very confusing how to navigate and setup and connect the hub and agent, on both sides.

Add the browse feature to when adding new server directly, let the user select the path or directory they want to browse and add it to the server, so the user doesn't have to add it manually, make it more user friendly and intuitive.
